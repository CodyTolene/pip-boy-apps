{
  "$schema": "http://json.schemastore.org/package",
  "name": "pip-boy-apps",
  "type": "module",
  "scripts": {
    "build": "node .scripts/generate-apps-local.js",
    "min": "node .scripts/minify.js",
    "min:watch": "node .scripts/minify-watch.js",
    "start": "npm run build && node .scripts/start.js",
    "prettier": "prettier --write .",
    "prettier:check": "prettier --check ."
  },
  "devDependencies": {
    "@types/node": "^24.1.0",
    "prettier": "^3.6.2"
  },
  "dependencies": {
    "espruino": "^0.1.67"
  }
}
